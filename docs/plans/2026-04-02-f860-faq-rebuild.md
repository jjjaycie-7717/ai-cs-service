# F860 FAQ Rebuild Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 用新的 `FAQ 知识_F860.xlsx` 全量替换旧 FAQ 数据，并生成新的结构化源文件、检索 chunks 和评测 queries。

**Architecture:** 先新增一个可重复执行的 FAQ 重建脚本，以 Excel 为输入生成 `data/faq_source.json`、`faq_chunks.jsonl` 和 `eval_queries.jsonl`。服务端读取入口和入库脚本保持不变，这样数据库只需要按现有流程重新灌库即可。

**Tech Stack:** Node.js、JSON、JSONL、现有检索脚本与 Qdrant 入库流程

---

### Task 1: 为 FAQ 重建脚本补测试

**Files:**
- Create: `scripts/rebuild_faq_data.test.js`
- Modify: `package.json`

**Step 1: Write the failing test**

编写一个最小测试，构造一份模拟 Excel 解析结果，验证生成后的 FAQ 源数据会：

- 生成稳定的 `faq_id`
- 正确保留 `category`、`question`、`answer`
- 按 `|` 拆分并去重 `similar_questions`
- 生成兼容现有服务的 `faq_chunks`
- 生成包含标准问题和相似问题的 `eval_queries`

**Step 2: Run test to verify it fails**

Run: `node --test scripts/rebuild_faq_data.test.js`

Expected: FAIL，因为重建脚本或可测试函数尚不存在。

**Step 3: Write minimal implementation**

新增一个 FAQ 重建脚本，至少导出可测试的纯函数：

- `normalizeFaqRows(rows)`
- `buildFaqChunks(faqs)`
- `buildEvalQueries(faqs)`

**Step 4: Run test to verify it passes**

Run: `node --test scripts/rebuild_faq_data.test.js`

Expected: PASS

### Task 2: 实现 Excel 读取与文件输出

**Files:**
- Create: `scripts/rebuild_faq_data.js`

**Step 1: Write the failing test**

在已有测试中增加一组输入，覆盖：

- 空白相似问题过滤
- 标准问题从相似问题中去重
- 多行答案保留

**Step 2: Run test to verify it fails**

Run: `node --test scripts/rebuild_faq_data.test.js`

Expected: FAIL，提示当前实现未覆盖新增行为。

**Step 3: Write minimal implementation**

在 `scripts/rebuild_faq_data.js` 中实现：

- 无三方依赖读取 `.xlsx`
- 解析首个 sheet
- 将 Excel 数据写入 `data/faq_source.json`
- 将 FAQ chunk 写入 `faq_chunks.jsonl`
- 将 query 写入 `eval_queries.jsonl`

并提供 CLI 默认读取桌面的 `FAQ 知识_F860.xlsx`，也支持命令行传入自定义路径。

**Step 4: Run test to verify it passes**

Run: `node --test scripts/rebuild_faq_data.test.js`

Expected: PASS

### Task 3: 执行全量重建并校验结果

**Files:**
- Modify: `data/faq_source.json`
- Modify: `faq_chunks.jsonl`
- Modify: `eval_queries.jsonl`

**Step 1: Run generation command**

Run: `node scripts/rebuild_faq_data.js "/Users/jaycie/Desktop/FAQ 知识_F860.xlsx"`

Expected: 成功输出 FAQ 数量、chunk 数量和 query 数量。

**Step 2: Verify generated files**

检查：

- `data/faq_source.json` 条数为 Excel FAQ 条数
- `faq_chunks.jsonl` 每条都含 `faq_id` 和 `chunk_text`
- `eval_queries.jsonl` 的 `expected_faq_id` 都能映射到 FAQ 源数据

**Step 3: Spot check generated content**

抽查若干条 FAQ 的分类、标准问题、相似问题和答案，确认没有串行、丢字段或乱码。

### Task 4: 补充执行入口并验证

**Files:**
- Modify: `package.json`
- Optional Modify: `README.md`

**Step 1: Add runnable script**

新增脚本命令，例如：

`"rebuild:faq": "node scripts/rebuild_faq_data.js"`

**Step 2: Verify command works**

Run: `npm run rebuild:faq -- "/Users/jaycie/Desktop/FAQ 知识_F860.xlsx"`

Expected: PASS，生成结果与直接执行 Node 命令一致。

**Step 3: Optional retrieval verification**

如环境具备服务和向量库，执行：

- `npm run eval:retrieval`
- 或重新执行 `npm run embed:qdrant`

确认新知识库可继续用于检索与入库。
